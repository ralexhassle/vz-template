export enum PATHS {}

export enum ERRORS {}

export enum STATUS {
  IDLE = "idle",
  PENDING = "pending",
  RESOLVED = "resolved",
  REJECTED = "rejected",
}

export enum PORTAL_ID {
  NAVIGATION = "navigation",
  FOOTER = "footer",
  MODAL = "modal",
}
